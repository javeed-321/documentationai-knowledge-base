---
title: "Python SDK"
description: "The Python package embeds directly in your agent process. The ContextGuard decorator is the primary interface."
---

The Python package embeds directly in your agent process. The `ContextGuard` decorator is the primary interface.

