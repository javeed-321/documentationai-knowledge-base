---
title: "Sessions"
description: "Endpoints: GET /v1/sessions — List sessions (filter: agent_role, status); GET /v1/sessions/{session_id} — Session detail and status; DELETE /v1/sessions/{session_id} —…"
---

**`GET /v1/sessions`**  
List sessions (filter: agent_role, status)

**`GET /v1/sessions/{session_id}`**  
Session detail and status

**`DELETE /v1/sessions/{session_id}`**  
Revoke a session immediately

