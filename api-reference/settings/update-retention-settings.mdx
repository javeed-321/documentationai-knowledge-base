---
title: "Update retention settings"
description: "Update retention settings (admin-scoped key required). Either field is optional — omit one to leave it unchanged. Set to 0 to disable automatic purge for that data type."
---
